process MyProcess = {

### keep the logging output to a nice level ###
      #include "FWCore/MessageLogger/data/MessageLogger.cfi"

      service = MessageLogger {
          untracked vstring destinations = { "cout" }
          untracked PSet cout = {
                    untracked string threshold = "DEBUG"
          }
#         untracked vstring debugModules = { "wMuNuSelFilter", "WMuNu_Analysis" }
          untracked vstring debugModules = { "WMuNu_Analysis" }
      }

      service = AdaptorConfig {}

      untracked PSet options = {
               untracked bool wantSummary = true
               untracked bool makeTriggerResults = true }

      untracked PSet maxEvents = { untracked int32 input = 500 }
      #untracked PSet maxEvents = { untracked int32 input = -1 }

      source = PoolSource {
            untracked vstring fileNames = {
                //16x
            '/store/mc/2007/10/19/CSA07-Wmunu-1192835438/0005/1809B1ED-4B89-DC11-A204-0019B9E4FD43.root'
            }
            untracked uint32 debugVerbosity = 0
            untracked bool   debugFlag     = false
      }

#------------ WToMuNu -------------------------------------
      # Selection for AN 2007/031
      include "ElectroWeakAnalysis/WMuNu/data/WMuNuSel_16x.cff"

#------------ W_Analyzer -------------------------------------
module WMuNu_Analysis = WMuNuAnalyzer {
      InputTag MuonTag = globalMuons
      InputTag METTag = met
      InputTag JetTag = iterativeCone5CaloJets
      InputTag IsolationTag = muGlobalIsoDepositCtfTk
      double PtThrForZCount = 20.
      double PtCut = 25.
      double EtaCut = 2.0
      double IsoCut = 3.0
      double IsoCone = 0.3
      double MassTMin = 50.
      double MassTMax = 200.
      double EJetMin = 40.
      int32 NJetMax = 3
      double AcopCut = 1.0

      untracked string rootFileName = "WMuNuPlots.root"
}

  path wmunu = { wmunusel, WMuNu_Analysis }

}
